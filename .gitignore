# Secrets & personal config
.env
config/candidate_profile.json

# Per-user data (resumes, profiles) — never commit personal info
config/users/

# SQLite database (contains personal data)
data/job_workflow.db
data/job_workflow.db-wal
data/job_workflow.db-shm

# Entire resume folder — personal files, keep off git
resume/

# Job history (machine-specific)
data/seen_jobs.json

# Logs
logs/*.log
logs/cron.log

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# macOS
.DS_Store
