{
  "_comment": "Add the 'job-hunt' block into your Claude Desktop config file.",
  "_config_file_location": {
    "macOS":   "~/Library/Application Support/Claude/claude_desktop_config.json",
    "Windows": "%APPDATA%\\Claude\\claude_desktop_config.json"
  },

  "mcpServers": {
    "job-hunt": {
      "command": "python3",
      "_command_note": "Use the same python that has your dependencies installed. Check with: which python3",
      "args": ["/ABSOLUTE/PATH/TO/job-workflow/src/mcp_server.py"],
      "_args_note": "Replace with the real absolute path to mcp_server.py on your machine.",
      "env": {
        "DISCORD_WEBHOOK_URL": "https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN"
      }
    }
  }
}
